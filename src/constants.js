/**
 * Here to replace env variable until i find a walktrough with Vite syntax
 * (to delete when i can use .env)
 * 
 * URL's API
 * @memberof Global
 */

export const BASE_URL = 'http://localhost:3001'